export namespace Gamers {
  export enum Role {
    CREATOR = 'creator',
    PARTICIPANT = 'participant',
    SILENT = 'silent',
  }
  export enum SuspectState {
    EXCLUDED = 'excluded',
    MAYBE = 'maybe',
    SURE = 'sure',
  }
}
